import React from 'react';

const Item = props =>{
    const {post} = props;
    return(
        <div>
            <h1>{Item.title}</h1>
            <p>{Item.title}</p>
        </div>
    )
}
export default Item;